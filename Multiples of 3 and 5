#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
cin.tie(NULL);
     int i,sum=0;
    for(i=0;i<1000;i++)
    {
        if(i%5==0||i%3==0)
        {
            sum+=1;
        }
    }
    cout<<sum;
    return 0;
}
